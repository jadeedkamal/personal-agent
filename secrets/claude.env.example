# Copy to ~/.secrets/claude.env (chmod 600) and fill in.
#
# Generate a token with: claude setup-token
# (run interactively, follow the browser login flow, then paste the printed token below)
CLAUDE_CODE_OAUTH_TOKEN=
