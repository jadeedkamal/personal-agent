# Moved

This file's content was merged into `docs/ARCHITECTURE.md` on 2026-09-10, as part of setting up
`docs/` + `memory/` as the app's persistent, provider-agnostic knowledge base. Read
`docs/ARCHITECTURE.md` instead — nothing was dropped, this file is kept only so old links to it
don't 404.
